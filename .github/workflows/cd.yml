name: cd

on:
  push:
    branches: [main]

  jobs:
    tests:
      name: Tests
      runs-on: debian-latest

      steps:
        - name: check out code
          uses: actions/checkout@v4
  
        - name: Set up Go
          uses: actions/setup-go@v5
          with: 
          go-version: 1.23.1

    Deploy:
      name: Deploy
      runs-on: debian-latest

      steps:
        - name: buildprod.sh
          uses: scripts/buildprod.sh
